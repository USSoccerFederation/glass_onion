# This is a Databricks asset bundle definition for glass_onion.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: glass_onion

workspace:
  host: REPLACE_WITH_WORKSPACE_URL
  root_path: /Packages/${bundle.name}

artifacts:
  default:
    files:
      - source: REPLACE_WITH_ARTIFACT_PATH